# REACTOR CLI.
    PLEASE FOR THE LOVE OF GOD AND ALL THE PEOPLE WITH HIM. READ THE DOCUMENTATION


    TO DO
        META - JAVASCRIPT
            CREATE - INIT javascript file - Checks for cookies. If it finds any. It will load values in it. Then it should have values which are simple. Like Temperature ETC.
            CREATE - TERMINAL parser- Loads the values in input field and checks the current subsys. if it matches. good if not. it will output error
            CREATE - Simple command Just two. Start, Stop. if start is initiated it will change value in init from 0 to 1. AKA bool
            CREATE - DESIGN DISPLAY - Runs every 1 Seconds. Just updates the values and colors on main display. Start simple. Just make it a function. Then in init it will be executed every 1 Second
        CSS - 
            CREATE - LAYOUT of the page
            CREATE - LIGHT x DARK THEME.
            CREATE - DEBUG THEME

            
          